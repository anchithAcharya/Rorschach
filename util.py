from point import Point


IMAGE_MODE = 'RGB' # b&w 1 bit

# a=Point('+', 0)
# b=Point(-5, 0)

# res = Point(0,0)

# match((a.x,b.x)):
#     case ('+', '-') | ('-', '+'): res.x = '.'
#     case ('+', _ as o):res.x = '+' if o >= 0 else o
#     case _: res.x = a.x+b.x
# 
# print(res)